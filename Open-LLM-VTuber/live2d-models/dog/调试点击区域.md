# Live2D 点击区域调试指南

## 📐 坐标系统详解

### Live2D坐标系规则
```
模型坐标系示意图：
       Y (+1.0)
          ↑
          |
(-1.0) ←--+--→ (+1.0) X
          |
          ↓
       Y (-1.0)
```

### 当前配置分析

#### 头部区域
```json
"HitAreaHead": {
    "X": 0.0,      // 中心：模型正中央
    "Y": 0.4,      // 中心：模型上半部分  
    "Width": 1.6,  // 宽度：左右各扩展0.8
    "Height": 1.0  // 高度：上下各扩展0.5
}
```
**覆盖范围计算**：
- 左边界：0.0 - 1.6/2 = -0.8
- 右边界：0.0 + 1.6/2 = +0.8
- 上边界：0.4 + 1.0/2 = +0.9  
- 下边界：0.4 - 1.0/2 = -0.1

#### 身体区域
```json
"HitAreaBody": {
    "X": 0.0,      // 中心：模型正中央
    "Y": -0.4,     // 中心：模型下半部分
    "Width": 2.2,  // 宽度：左右各扩展1.1
    "Height": 2.0  // 高度：上下各扩展1.0
}
```
**覆盖范围计算**：
- 左边界：0.0 - 2.2/2 = -1.1
- 右边界：0.0 + 2.2/2 = +1.1
- 上边界：-0.4 + 2.0/2 = +0.6
- 下边界：-0.4 - 2.0/2 = -1.4

## 🔍 问题排查步骤

### Step 1: 检查服务器日志
启动服务器后，点击模型观察日志输出：
```bash
uv run run_server.py
# 观察是否有以下日志：
# Live2D tap on HitAreaHead: triggering 'Nod' motion
# Live2D tap on HitAreaBody: triggering 'Sway' motion
```

### Step 2: 测试极大区域
如果完全没反应，先用这个配置测试：
```json
"HitAreas": [
    {
        "Name": "HitAreaHead", 
        "Id": "HitAreaHead",
        "X": 0.0, "Y": 1.0,
        "Width": 3.0, "Height": 2.0
    },
    {
        "Name": "HitAreaBody",
        "Id": "HitAreaBody", 
        "X": 0.0, "Y": -0.5,
        "Width": 4.0, "Height": 3.0
    }
]
```

### Step 3: 分别测试左右区域
测试左半边：
```json
"HitAreaBody": {
    "X": -0.6, "Y": 0.0,
    "Width": 1.0, "Height": 2.0
}
```

测试右半边：
```json
"HitAreaBody": {
    "X": 0.6, "Y": 0.0, 
    "Width": 1.0, "Height": 2.0
}
```

## 🎯 常见问题解决

### 问题1：只有右半边能点击
**原因**：X坐标偏正，或宽度不够
**解决**：
- 确保 X = 0.0 (居中)
- 增加 Width 值
- 检查模型实际坐标范围

### 问题2：点击区域超出模型范围
**原因**：坐标值过大
**解决**：
- Live2D坐标通常在 -2.0 到 +2.0 范围内
- 避免使用 >10 的数值

### 问题3：头部和身体重叠
**解决**：调整Y坐标分离区域
```json
// 头部：Y = 0.5 以上
// 身体：Y = 0.0 以下
```

## 🔧 快速配置模板

### 保守配置（确保能点击）
```json
"HitAreas": [
    {
        "Name": "HitAreaHead",
        "Id": "HitAreaHead",
        "X": 0.0, "Y": 0.6,
        "Width": 2.0, "Height": 1.2
    },
    {
        "Name": "HitAreaBody", 
        "Id": "HitAreaBody",
        "X": 0.0, "Y": -0.2,
        "Width": 3.0, "Height": 2.5
    }
]
```

### 精确配置（最佳体验）
```json
"HitAreas": [
    {
        "Name": "HitAreaHead",
        "Id": "HitAreaHead", 
        "X": 0.0, "Y": 0.4,
        "Width": 1.6, "Height": 1.0
    },
    {
        "Name": "HitAreaBody",
        "Id": "HitAreaBody",
        "X": 0.0, "Y": -0.4, 
        "Width": 2.2, "Height": 2.0
    }
]
```

## 📝 调试记录模板

记录你的测试结果：
```
配置：X=?, Y=?, Width=?, Height=?
结果：
- 左侧点击：✅/❌
- 右侧点击：✅/❌  
- 中央点击：✅/❌
- 服务器日志：有/无反应
``` 