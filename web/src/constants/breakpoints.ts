/** Responsive breakpoints (px) shared across the app. */
export const MOBILE_BREAKPOINT = 768;
export const SPLIT_BREAKPOINT = 1024;
/** Hysteresis: split→overlay at SPLIT_BREAKPOINT, overlay→split at SPLIT_BREAKPOINT + this offset */
export const SPLIT_HYSTERESIS = 50;
