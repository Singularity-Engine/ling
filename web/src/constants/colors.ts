/**
 * 通用半透明颜色常量
 *
 * 用于维护整个应用的颜色一致性，避免重复的硬编码颜色值。
 * 这些颜色主要用于遮罩、背景和边框等半透明效果。
 */

// ── 黑色半透明色系 (用于遮罩、背景) ──
export const OVERLAY_COLORS = {
  /** 轻微遮罩 - rgba(0, 0, 0, 0.18) */
  LIGHT: 'rgba(0, 0, 0, 0.18)',
  /** 中等遮罩 - rgba(0, 0, 0, 0.35) */
  MEDIUM: 'rgba(0, 0, 0, 0.35)',
  /** 较深遮罩 - rgba(0, 0, 0, 0.6) */
  DARK: 'rgba(0, 0, 0, 0.6)',
  /** 深度遮罩 - rgba(0, 0, 0, 0.85) */
  DEEP: 'rgba(0, 0, 0, 0.85)',
} as const;

// ── 白色半透明色系 (用于按钮、边框) ──
export const WHITE_ALPHA = {
  /** 极浅透明白 - rgba(255, 255, 255, 0.05) */
  SUBTLE: 'rgba(255, 255, 255, 0.05)',
  /** 浅透明白 - rgba(255, 255, 255, 0.06) */
  LIGHT_BORDER: 'rgba(255, 255, 255, 0.06)',
  /** 按钮背景 - rgba(255, 255, 255, 0.08) */
  BUTTON_BG: 'rgba(255, 255, 255, 0.08)',
  /** 边框色 - rgba(255, 255, 255, 0.12) */
  BORDER: 'rgba(255, 255, 255, 0.12)',
  /** 稍深边框 - rgba(255, 255, 255, 0.15) */
  BORDER_HOVER: 'rgba(255, 255, 255, 0.15)',
} as const;

// ── 其他常用半透明色 ──
export const MISC_COLORS = {
  /** 错误信息背景 - rgba(0, 0, 0, 0.3) */
  ERROR_BG: 'rgba(0, 0, 0, 0.3)',
} as const;